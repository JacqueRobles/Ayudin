/**
 * Archivo índice para estilos
 * Exporta todos los tokens y utilidades de estilos
 */

export { tokens } from '@/constants/tokens';
export {
  colors,
  spacing,
  layout,
  shadows,
  typography,
  combineStyles,
  conditionalStyles,
  dynamicStyles,
} from './utils';

// Tipos para mejor intellisense
export type { TokenColors, TokenSpacing, TokenBorderRadius, TokenTypography, TokenSizes } from '@/constants/tokens';


